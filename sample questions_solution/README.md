#Few Correction in quiz answers

1. startActivityForResult() is asynchronous and two way( sends the result back to calling activity)
2. onStop() and onDestroy() may not be called in low memory situations.